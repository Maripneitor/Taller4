cd ~/hexagonal/taller4

#Iniciar el servicio de docker compose#
docker compose up --build

#Iniciar el servicio de docker compose en segundo plano#
docker compose up --build -d

#Instalar librerías: Agregar a requirements.txt -> docker compose up --build #

#Detener el servicio de docker compose#
docker compose down
